FROM scratch
# This is a placeholder Dockerfile at the root of the repository.
# Please use the Dockerfiles in ./Backend and ./Frontend for building the respective services.
# For multi-service builds, use docker-compose.yml at the root. 